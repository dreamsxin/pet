Create one horizontal animation strip for Codex pet `capy-lulu`, state `running-right`.

Use the attached canonical base for identity. Use the attached layout guide only for slot count, spacing, centering, and padding; do not draw the guide.

Output exactly 8 full-body frames in one left-to-right row on flat pure magenta #FF00FF. Treat the row as 8 invisible equal-width slots: one centered complete pose per slot, evenly spaced, with no overlap, clipping, empty slots, labels, or borders.
CRITICAL: every frame must remain fully inside its own slot with visible padding on all sides. Do not let ears, fruit, feet, muzzle, sleeves, or any body part touch or cross a slot boundary. Do not let any part of one frame spill into the neighboring slot.

Identity: same pet in every frame: cute capybara-like mascot, oversized orange muzzle, yellow body, blue sailor outfit, red neckerchief, orange fruit on head, compact full-body silhouette, clean 3D toy style. Preserve silhouette, face, proportions, markings, palette, material, style, and props.
Animation continuity: keep apparent pet scale and baseline stable within the row. Move the pose within the slot instead of redrawing the pet larger or smaller frame to frame.

State action: Running-right loop: a cheerful small running cycle traveling to the right through clear alternating stride poses.

State requirements:
- Show an unmistakable small running cycle to the right with alternating left/right leg extension, body lean, and arm swing.
- The row must clearly face and travel right, not just slide sideways.
- Use visible stride contrast: contact, push-off, passing, and extended stride poses across the 8 frames.
- Enforce a clear 8-frame gait cycle: frame 1 right foot forward, frame 2 passing pose, frame 3 left foot forward, frame 4 passing pose, then repeat with matching mirrored cadence across frames 5-8.
- The front and back legs must alternate clearly; do not keep both feet in nearly the same pose across adjacent frames.
- The arms or sleeves should also alternate with the legs so the cycle reads as a true run instead of a body slide.
- Add stronger up/down body bounce and forward lean so it reads as running at pet size.
- Keep the apparent pet size slightly smaller than the slot limit so the widest stride frames still fit fully inside the slot.
- Do not draw speed lines, dust clouds, floor shadows, motion trails, or detached motion effects.
